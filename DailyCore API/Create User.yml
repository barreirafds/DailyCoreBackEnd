info:
  name: Create User
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/api/users
  body:
    type: json
    data: |-
      {
        "name": "Diego",
        "email": "diego@email.com",
        "password": "123456",
        "phoneNumber": "912345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
